# Supabase Configuration (local — run `npm run db:start` to get these values)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<from npm run db:start output>
VITE_APP_URL=http://localhost:5173

# WhatsApp Bot (edge functions)
GROQ_API_KEY=your_groq_api_key
TWILIO_AUTH_TOKEN=your_twilio_auth_token
WHATSAPP_ADMIN_NUMBERS=whatsapp:+18005551234
TWILIO_SKIP_SIGNATURE_VALIDATION=true

# Podman users: uncomment and set your user ID (run `id -u` to find it)
# DOCKER_HOST=unix:///run/user/<your-user-id>/podman/podman.sock
